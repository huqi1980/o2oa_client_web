language: node_js
node_js:
  - "7"

install: true

before_script:
  - npm install -g gulp-cli
  - npm i -g karma-cli
  - npm install -g codecov

script:
  - gulp
  - karma run
  - npm run report-coverage

after_success:
  - codecov